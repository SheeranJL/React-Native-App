import {StyleSheet} from 'react-native';

export const headerStyles = StyleSheet.create({

  logoutTextContainer: {
    display: 'flex',
    alignItems: 'center',
  },

  logoutText: {
    fontSize: 20,
    color: 'red',
    marginRight: 10,
  }

})
